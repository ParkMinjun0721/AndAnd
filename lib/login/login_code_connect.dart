import 'package:flutter/material.dart';

class LoginCodeConnect extends StatelessWidget {
  const LoginCodeConnect({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: SingleChildScrollView(
        child: Column(
          children: [
            Container(
              child: Image.asset('assets/bear_use_labtop.png'),
            )
          ],
        ),
      ),
    );
  }
}
